# --- NEVER COMMIT: PII, live credentials, proprietary legacy source ---
# Legacy source tree + 7.3GB SQL Server .bak (source of truth stays local)
platform/original-source/
# Extracted config CSVs contain live legacy web-service credentials
platform/restore-db/extracted/
# Golden-master sessions contain real names/emails (PII)
platform/restore-db/goldens/

# macOS
.DS_Store

# IDE / editor
.idea/
.vscode/

# Dependencies & build output (Laravel/React, added in phase 1)
/api/vendor/
/api/node_modules/
/api/.env
/api/.env.*.local
/api/storage/*.key
/panel/node_modules/
/panel/dist/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
development-assets/Firebase Orders/
